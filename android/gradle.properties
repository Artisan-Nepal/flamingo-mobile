# 1536M OOMs during release builds (Jetifier native-lib transform). 4G is the
# common setting for Flutter release builds.
org.gradle.jvmargs=-Xmx4096M -XX:MaxMetaspaceSize=512m
kotlin.jvm.target.validation.mode=warning
android.useAndroidX=true
android.enableJetifier=true

# Release signing moved to android/key.properties (gitignored) - see
# key.properties.example. Do not put keystore passwords here.

MAPS_API_KEY=AIzaSyDNj4Zb6htfMVl26pxo55bUGKZ4nMMZRNU
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
