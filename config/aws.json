{
  "_comment": "Production API, served via Cloudflare in front of AWS App Runner. This is a stable custom domain, unlike the old awsapprunner.com URL - no need to update after redeploys.",
  "API_BASE_URL": "https://api.flamingo.com.np/api"
}
